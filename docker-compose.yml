version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8010-8999:8010-8999"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - PORT=0
      - HOST=0.0.0.0
